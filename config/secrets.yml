# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rake secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

development:
  admin_name: First User
  admin_email: user@example.com
  admin_password: changeme
  email_provider_username: <%= ENV["GMAIL_USERNAME"] %>
  email_provider_password: <%= ENV["GMAIL_PASSWORD"] %>
  domain_name: example.com
  mailchimp_api_key: <%= ENV.fetch("MAILCHIMP_API_KEY") %>
  mailchimp_list_id: <%= ENV.fetch("MAILCHIMP_LIST_ID") %>
  secret_key_base: 3f52514315473203803399028e38cd267445ac9c1b81e6b9b4890ea33739f402762b765f8c4105a990730d92290c3a9a10a63f5c7720edf4d47e44d533e1cae5

test:
  domain_name: example.com 
  secret_key_base: e6ae66283f769ddbf8b7443262fa2ced56fafed88d05e783b67573614e39de9dacaa7d069a08312fc16e595e1d865822e9dca29ee0d7c84106248a7de4a5e0c9

# Do not keep production secrets in the repository,
# instead read values from the environment.
production:
  admin_name: <%= ENV["ADMIN_NAME"] %>
  admin_email: <%= ENV["ADMIN_EMAIL"] %>
  admin_password: <%= ENV["ADMIN_PASSWORD"] %>
  email_provider_username: <%= ENV["GMAIL_USERNAME"] %>
  email_provider_password: <%= ENV["GMAIL_PASSWORD"] %> 
  domain_name: <%= ENV["DOMAIN_NAME"] %>
  mailchimp_api_key: <%= ENV.fetch("MAILCHIMP_API_KEY") %> 
  mailchimp_list_id: <%= ENV.fetch("MAILCHIMP_LIST_ID") %> 
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
